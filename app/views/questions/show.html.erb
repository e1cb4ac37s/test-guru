<div class="<%= controller_name %> <%= action_name %>">
  <div class="question">
    <h3 class="question__title"><%= @question.body %></h3>
    <ol>
      <% @question.answers.each do |answer| %>
        <li><p><%= answer.body %></p></li>
      <% end %>
    </ol>
  </div>
</div>