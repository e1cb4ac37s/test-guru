<%= form_with model: [question, answer] do |form| %>
  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="form-group">
    <%= form.label :correct %>
    <%= form.check_box :correct %>
  </div>

  <%= form.submit %>
<% end%>